"""Route blueprints, extracted incrementally from app.py.

See docs/migration-runbook.md for the session-by-session plan. Each
blueprint here owns a set of routes with URLs unchanged from their old
app.py-level definitions (no url_prefix)."""
